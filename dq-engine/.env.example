# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration (SQLite)
# DATA_DIR=/path/to/data/directory (optional - defaults to dq-engine/data)
# Database file will be created at: DATA_DIR/iws-dashboard.db

# Encryption Key (REQUIRED for production)
# Generate a secure key by running: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Twilio Configuration (SMS & WhatsApp)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_FROM=whatsapp:+1234567890

# Redis Configuration (for Bull queue - optional, uses in-memory if not set)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password